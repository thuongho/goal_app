<h1>Listing goals</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Pledged Date</th>
      <th>Expire</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= goal.name %></td>
        <td><%= goal.description %></td>
        <td><%= goal.created_at %></td>
        <td><%= goal.expire %></td>
        <td><%= link_to 'Show', user_goal_path(goal.user_id, goal.id) %></td>
        <td><%= link_to 'Edit', edit_user_goal_path(goal.user_id, goal.id) %></td>
        <td><%= link_to 'Destroy', user_goal_path(goal.user_id, goal.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Goal', new_user_goal_path %>
